// route handlers are only needed when an outside server wants to
// access our app programmatically

// this is equivalent to below
// import { GET, POST } from "@/auth";
// export { GET, POST };

export { GET, POST } from "@/auth";
